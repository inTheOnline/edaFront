# outgoing 模块主页

## 目录说明
- `index.vue`：外发模块首页（可视化 + 工具入口 + 待办预留）
- `outform`：外发录表
- `outback`：外发回货
- `supWork`：供应商工费（本轮未改动）
- `service.ts`：outform/outback 统一请求入口

## 本轮更新（2026-04）
- 恢复并保留首页工具入口中原有可用能力（供应商扣款处理、供应商报价导入）
- `outform`：
- 批量新增新增“前缀+单号”拼接规则
- 操作列“查看”改为大窗显示回货明细
- 删除确认改为大窗显示关联回货明细并二次确认
- `outback`：
- 批量选择外发明细支持多选并批量带入
- `state` 字典统一归一化处理，修复状态列和状态下拉空白问题

## 接口文档
- 以 [API.md](./API.md) 为准
