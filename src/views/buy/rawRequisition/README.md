# 原材料请购

## 状态规则

- 状态按 `待处理 → 部分采购 → 已采购 → 部分回执 → 已完成` 流转。
- 同时存在未采购和已回执数量时，优先显示“部分回执”。
- 主表、明细表和查看弹窗中的状态统一使用语义 Tag 展示。

- 路由：`/buy/rawRequisition`
- 默认展示请购主表，可切换为请购明细。
- 查看按钮展示整单明细，新增、编辑按整单保存，单号由后端生成。
